/* =========================================================
   ADMIN INVENTORY - OUTLET PAGE
========================================================= */

.inventory-dashboard {
  width: 100%;
  min-height: 100%;
  padding: 24px 28px 40px;
  background: #f7faf8;
  box-sizing: border-box;
  color: #17231d;
}

/* =========================================================
   PAGE HEADER
========================================================= */

.inventory-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20px;
  margin-bottom: 22px;
}

.inventory-header-left h1 {
  margin: 0;
  font-size: 24px;
  font-weight: 700;
  color: #14231b;
  letter-spacing: -0.3px;
}

.inventory-header-left p {
  margin: 5px 0 0;
  color: #7a877f;
  font-size: 13px;
}

.inventory-header-icon {
  width: 42px;
  height: 42px;
  border-radius: 12px;
  background: #e8f8ef;
  color: #16a34a;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
}

/* =========================================================
   STAT CARDS
========================================================= */

/* .inventory-cards {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
  margin-bottom: 20px;
} */

/* .inventory-stat-card {
  position: relative;
  min-height: 105px;
  margin-right: 10px;
  width: 200px;
  padding: 17px 18px;
  border: 1px solid #e8eee9;
  border-radius: 14px;
  background: #ffffff;
  box-shadow: 0 3px 14px rgba(30, 55, 40, 0.05);
  overflow: hidden;
  transition: 0.2s ease;
} */

/* Card container ko flex karo taaki card apni marzi se stretch na ho */
.inventory-cards {
  display: flex !important;
  flex-wrap: wrap !important;
  gap: 16px !important;
  width: 100% !important;
  margin-bottom: 20px;
}

/* Card ko zabardasti 4-sided barabar square dabba banane ke liye */
.inventory-cards > div,
.inventory-stat-card {
  width: 180px !important;
  max-width: 180px !important;
  min-width: 180px !important;
  height: 180px !important;
  max-height: 180px !important;
  min-height: 180px !important;
  
  box-sizing: border-box !important;
  margin: 0 !important;
  padding: 16px !important;
  
  display: flex !important;
  flex-direction: column !important;
  justify-content: space-between !important;
  
  border: 1px solid #e8eee9 !important;
  border-radius: 14px !important;
  background: #ffffff !important;
}

.inventory-stat-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 7px 20px rgba(30, 55, 40, 0.08);
}

.inventory-stat-card::after {
  content: "";
  position: absolute;
  right: -20px;
  bottom: -25px;
  width: 75px;
  height: 75px;
  border-radius: 50%;
  background: rgba(34, 197, 94, 0.06);
}

/* .inventory-stat-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
} */
.inventory-cards .card {
  border: black solid 2px;
}
.inventory-stat-card h3 {
  margin: 0;
  font-size: 12px;
  font-weight: 600;
  color: #738078;
  text-transform: uppercase;
  letter-spacing: 0.35px;

}

.inventory-stat-card h2 {
  margin: 8px 0 0;
  font-size: 27px;
  line-height: 1;
  font-weight: 800;
  color: #18251e;
}

.inventory-stat-icon {
  width: 34px;
  height: 34px;
  border-radius: 9px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
}

/* Stat colors */

.inventory-stat-card.total .inventory-stat-icon {
  background: #edf4ff;
  color: #2563eb;
}

.inventory-stat-card.stock .inventory-stat-icon {
  background: #eaf9f0;
  color: #16a34a;
}

.inventory-stat-card.low .inventory-stat-icon {
  background: #fff7df;
  color: #d97706;
}

.inventory-stat-card.out .inventory-stat-icon {
  background: #fff0f0;
  color: #dc2626;
}

.inventory-stat-card.total-stock .inventory-stat-icon {
  background: #f2edff;
  color: #7c3aed;
}

/* =========================================================
   INVENTORY CONTENT BOX
========================================================= */

.inventory-content {
  background: #ffffff;
  border: 1px solid #e8eee9;
  border-radius: 15px;
  box-shadow: 0 3px 15px rgba(30, 55, 40, 0.05);
  overflow: hidden;
}

/* =========================================================
   TABLE TOOLBAR
========================================================= */

.inventory-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
  padding: 16px 18px;
  border-bottom: 1px solid #edf1ee;
}

.inventory-toolbar-left {
  display: flex;
  align-items: center;
  gap: 9px;
}

.inventory-toolbar-left h2 {
  margin: 0;
  font-size: 16px;
  font-weight: 700;
  color: #17231d;
}

.inventory-toolbar-left span {
  padding: 3px 8px;
  border-radius: 20px;
  background: #eaf8ef;
  color: #159447;
  font-size: 10px;
  font-weight: 700;
}

/* =========================================================
   SEARCH + FILTER
========================================================= */

.inventory-top {
  display: flex;
  align-items: center;
  gap: 10px;
  margin: 0;
}

.inventory-search {
  position: relative;
}

.inventory-search input,
.inventory-top input {
  width: 280px;
  height: 38px;
  padding: 0 13px;
  border: 1px solid #dfe7e1;
  border-radius: 8px;
  outline: none;
  background: #fbfcfb;
  color: #27332c;
  font-size: 12px;
  transition: 0.2s ease;
  box-sizing: border-box;
}

.inventory-search input:focus,
.inventory-top input:focus {
  border-color: #22c55e;
  background: #ffffff;
  box-shadow: 0 0 0 3px rgba(34, 197, 94, 0.08);
}

.inventory-top select {
  width: 145px;
  height: 38px;
  padding: 0 10px;
  border: 1px solid #dfe7e1;
  border-radius: 8px;
  outline: none;
  background: #fbfcfb;
  color: #455149;
  font-size: 12px;
  cursor: pointer;
}

.inventory-top select:focus {
  border-color: #22c55e;
}

/* =========================================================
   TABLE WRAPPER
========================================================= */

.inventory-table-wrapper {
  width: 100%;
  overflow-x: auto;
}

/* =========================================================
   INVENTORY TABLE
========================================================= */

.inventory-table {
  width: 100%;
  min-width: 850px;
  border-collapse: collapse;
  background: #ffffff;
}

.inventory-table th {
  padding: 11px 14px;
  background: #f7faf8;
  color: #68756d;
  border-bottom: 1px solid #e8eee9;
  text-align: left;
  white-space: nowrap;

  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.45px;
}

.inventory-table td {
  padding: 10px 14px;
  border-bottom: 1px solid #f0f3f1;
  color: #344139;

  font-size: 11px;
  font-weight: 500;
  vertical-align: middle;
  white-space: nowrap;
}

.inventory-table tbody tr {
  transition: background 0.15s ease;
}

.inventory-table tbody tr:hover {
  background: #f8fcf9;
}

.inventory-table tbody tr:last-child td {
  border-bottom: none;
}

/* =========================================================
   PRODUCT CELL
========================================================= */

.inventory-product {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 180px;
}

.inventory-product-info {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.inventory-product-name {
  color: #17231d;
  font-size: 12px;
  font-weight: 650;
}

.inventory-product-category {
  color: #8a958e;
  font-size: 10px;
}

/* =========================================================
   PRODUCT IMAGE
========================================================= */

.inventory-image {
  width: 42px;
  height: 42px;
  flex: 0 0 42px;
  object-fit: cover;
  border-radius: 8px;
  border: 1px solid #e7ece8;
  background: #f7f9f7;
}

/* =========================================================
   STOCK STATUS
========================================================= */

.status {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 76px;
  padding: 5px 9px;
  border-radius: 20px;

  font-size: 10px;
  font-weight: 700;
  line-height: 1;
  white-space: nowrap;
}

.in-stock {
  background: #e9f9ef;
  color: #16803c;
}

.low-stock {
  background: #fff6dc;
  color: #b46a00;
}

.out-stock {
  background: #ffeded;
  color: #c62828;
}

/* =========================================================
   STOCK ROW HIGHLIGHTS
========================================================= */

.low-stock-row {
  background: #fffdf6;
}

.out-stock-row {
  background: #fffafa;
}

/* =========================================================
   WARNING / DANGER
========================================================= */

.warning {
  margin-top: 3px;
  color: #c77a00;
  font-size: 9px;
  font-weight: 500;
}

.danger {
  margin-top: 3px;
  color: #d32f2f;
  font-size: 9px;
  font-weight: 600;
}

/* =========================================================
   STOCK ACTION BUTTONS
========================================================= */

.inventory-actions {
  display: flex;
  align-items: center;
  gap: 5px;
}

.add-btn,
.remove-btn {
  min-width: 30px;
  height: 28px;
  padding: 0 9px;

  border: none;
  border-radius: 6px;

  font-size: 11px;
  font-weight: 700;
  cursor: pointer;
  transition: 0.18s ease;
}

.add-btn {
  background: #e9f9ef;
  color: #16803c;
}

.add-btn:hover {
  background: #16a34a;
  color: #ffffff;
}

.remove-btn {
  background: #ffeded;
  color: #dc2626;
}

.remove-btn:hover {
  background: #dc2626;
  color: #ffffff;
}

/* =========================================================
   PRICE / STOCK NUMBERS
========================================================= */

.inventory-price {
  color: #17231d;
  font-size: 11px;
  font-weight: 700;
}

.inventory-stock-number {
  color: #26352c;
  font-size: 12px;
  font-weight: 700;
}

/* =========================================================
   EMPTY STATE
========================================================= */

.inventory-empty {
  padding: 45px 20px;
  text-align: center;
  color: #89958d;
  font-size: 12px;
}

/* =========================================================
   PAGINATION / FOOTER IF PRESENT
========================================================= */

.inventory-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 18px;
  border-top: 1px solid #edf1ee;
  color: #849087;
  font-size: 10px;
}

/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width: 1250px) {
  .inventory-cards {
    grid-template-columns: repeat(3, 1fr);
  }

  .inventory-dashboard {
    padding: 20px;
  }
}

@media (max-width: 900px) {
  .inventory-cards {
    grid-template-columns: repeat(2, 1fr);
  }

  .inventory-header {
    align-items: flex-start;
  }

  .inventory-toolbar {
    flex-direction: column;
    align-items: stretch;
  }

  .inventory-top {
    width: 100%;
  }

  .inventory-top input {
    flex: 1;
    width: auto;
  }

  .inventory-top select {
    width: 160px;
  }
}

@media (max-width: 600px) {
  .inventory-dashboard {
    padding: 15px;
  }

  .inventory-header-left h1 {
    font-size: 20px;
  }

  .inventory-cards {
    grid-template-columns: 1fr;
    gap: 10px;
  }

  .inventory-stat-card {
    min-height: 90px;
  }

  .inventory-toolbar {
    padding: 13px;
  }

  .inventory-top {
    flex-direction: column;
    align-items: stretch;
  }

  .inventory-top input,
  .inventory-top select {
    width: 100%;
  }
}